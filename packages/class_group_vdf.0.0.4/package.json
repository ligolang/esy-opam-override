{
  "build": [
    [
      "bash",
      "-c",
      "#{os == 'windows' ? 'patch -p1 < winpatch.patch' : 'true' }"
    ],
    "env PATH=#{yarn-pkg-config.bin:$PATH} dune build -p class_group_vdf"
  ],
  "dependencies": {
    "esy-gmp": "*",
    "yarn-pkg-config": "*"
  }
}
